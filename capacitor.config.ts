import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'us.cvbn.buttonquest',
  appName: 'button-quest',
  webDir: 'dist/button-quest',
  bundledWebRuntime: false
};

export default config;
